namespace TechTest
{
    // NOTE: Arguably no need for this to be an interface but if we were to
    //       unit test this, it would make life simpler
    public interface IBooksImporter
    {
        void Import();
    }
}
